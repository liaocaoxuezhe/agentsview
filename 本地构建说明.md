# AgentsView 本地构建与启动说明

本文说明如何在本机从源码构建、开发与启动 AgentsView。
**以仓库 `Makefile` 为准**；根目录的 `.command` 脚本只是 macOS 便捷封装，
内部调用的也是同一套目标。

正式用户文档见 [agentsview.io](https://agentsview.io) 与
`docs/quickstart.md`。

---

## 1. 环境要求

| 依赖 | 版本 / 说明 |
| --- | --- |
| Go | **1.26+**，且 **CGO 可用**（SQLite / FTS5） |
| Node.js | **22+**（前端 `engines` 也接受部分 20.x / 24.x，官方推荐 22+） |
| npm | 随 Node 安装；前端依赖含公开 git 仓库 `@kenn-io/kit-ui` |
| make | macOS 自带或 GNU make |
| C 编译器 | CGO 需要（macOS 装 Xcode CLT：`xcode-select --install`） |
| air（仅开发热重载） | `make air-install` 安装 `github.com/air-verse/air` |

构建标签与要点：

- 必须 `CGO_ENABLED=1`
- 必须带 `-tags fts5`（`make build` / `make dev` 已处理）
- 前端构建产物嵌入 `internal/web/dist/`，再打进 Go 二进制

检查版本示例：

```bash
go version    # 期望 go1.26+
node -v       # 期望 v22+
make help     # 列出全部构建目标
```

---

## 2. 推荐工作流一览

| 场景 | 命令 | 访问地址 |
| --- | --- | --- |
| 完整构建（内嵌前端） | `make build` | 再执行 `./agentsview serve` → `:8080` |
| 安装到本机 PATH | `make install` | `agentsview serve` |
| 后端开发（热重载） | `make dev` | `:8080` |
| 前端开发（Vite） | `make frontend-dev` | `:5173`（代理 `/api` → 后端） |
| 日常本地用构建产物 | `./agentsview serve` 或 `start-agentsview.command` | `:8080` |
| 日常改前后端 | 两终端 `make dev` + `make frontend-dev`，或 `start-agentsview-dev.command` | **打开 `:5173`** |

生产/自用跑服务，用**构建后的二进制 + `serve`**。
改 UI / API 时，用 **`make dev` + `make frontend-dev`**，不要再用「只起一个旧二进制」冒充开发模式。

---

## 3. 从零构建

在仓库根目录：

```bash
git clone <本仓库地址>
cd agentsview

# 构建前端并编译带嵌入资源的二进制 → ./agentsview
make build

# 可选：安装到 ~/.local/bin 或 $GOBIN / $GOPATH/bin
make install
```

`make build` 会依次：

1. 准备 sqlite 头文件与 LiteLLM pricing 快照（`pricing-snapshot`）
2. `cd frontend && npm ci && npm run build`
3. 拷贝 `frontend/dist` → `internal/web/dist`
4. `CGO_ENABLED=1 go build -tags fts5 ... -o agentsview ./cmd/agentsview`

仅重建前端：

```bash
make frontend
```

仅 release 优化构建：

```bash
make build-release
```

---

## 4. 启动方式（构建产物）

### 4.1 前台（推荐调试）

```bash
./agentsview serve
# 或已 install 后:
agentsview serve
```

- 默认监听 `http://127.0.0.1:8080`
- 会发现本机各 Agent 会话目录并同步到 `~/.agentsview/`
- 前台运行，日志打在终端，`Ctrl+C` 结束

常用参数：

```bash
agentsview serve --port 9090
agentsview serve --no-browser
agentsview serve --public-url https://your-forwarded-origin.example
```

### 4.2 后台 / 守护进程

长期后台用官方 daemon（读配置与环境变量，不接受 serve 专用 flag）：

```bash
agentsview daemon start
agentsview daemon status
agentsview daemon restart
agentsview daemon stop
```

需要 serve 专用 flag 的一次性后台（例如 `--no-sync`）：

```bash
agentsview serve --background --no-sync
agentsview serve status
agentsview serve stop
```

### 4.3 macOS 双击脚本

| 脚本 | 作用 |
| --- | --- |
| `start-agentsview.command` | 无二进制则 `make build`，再 `./agentsview serve` |
| `start-agentsview-dev.command` | `make dev` + `make frontend-dev`，打开 Vite 地址 |

旧版脚本曾手写 PID 文件、固定 `nohup` 起静态二进制，**已与当前 Makefile / CLI 不一致**。
现脚本改为调用上述官方目标；日常开发请以本文第 5 节为准。

---

## 5. 开发模式（前后端热更新）

需要**两个进程**（两个终端，或用 `start-agentsview-dev.command`）：

**终端 A — Go 后端 + air**

```bash
# 首次
make air-install

make dev
```

- 通过 `.air.toml` 调用 `scripts/dev-backend-build.sh`
- 产物在 `tmp/agentsview`，参数为 `serve`
- 修改 `.go` 等文件会自动重编译并重启

**终端 B — Vite 前端**

```bash
# 首次如无 node_modules
cd frontend && npm ci && cd ..

make frontend-dev
```

- 默认 `http://127.0.0.1:5173`
- `/api` 代理到 `VITE_API_TARGET`（默认 `http://127.0.0.1:8080`）

**开发时请打开 `:5173`**，不要只开 `:8080` 的旧嵌入页面（除非你在验证 embed 构建）。

可选：用生产库只读快照起服务（不写生产 DB）：

```bash
make dev-snapshot
# 可调: PROD_DATA_DIR=... SNAPSHOT_DIR=tmp/prod-snapshot RESNAPSHOT=0
```

---

## 6. 桌面端（Tauri，可选）

```bash
make desktop-dev          # 开发
make desktop-macos-app    # 仅 macOS .app
make desktop-build        # 全平台 bundle（视本机工具链而定）
```

细节见 `desktop/README.md`。

---

## 7. 测试与校验

```bash
make test        # Go 单测（CGO + fts5）
make test-short  # 快速子集
make vet
make lint        # golangci-lint + NilAway
make e2e         # Playwright（需前端与 e2e 环境）
```

PostgreSQL 集成测试：

```bash
make test-postgres
make postgres-down   # 用完停容器
```

首次装 git hooks（可选）：

```bash
make lint-tools
make install-hooks
```

---

## 8. 数据目录与环境变量

| 项 | 默认 / 说明 |
| --- | --- |
| 数据目录 | `~/.agentsview/`（可用 `AGENTSVIEW_DATA_DIR` 覆盖） |
| SQLite 归档 | `$AGENTSVIEW_DATA_DIR/sessions.db` |
| 配置 | `$AGENTSVIEW_DATA_DIR/config.toml` |
| 遥测 | `AGENTSVIEW_TELEMETRY_ENABLED=0` 可关闭 |
| 禁止自动起 daemon | `AGENTSVIEW_NO_DAEMON=1`（脚本/CI） |

开发时若需隔离数据，避免写坏本机归档：

```bash
export AGENTSVIEW_DATA_DIR="$PWD/tmp/dev-data"
mkdir -p "$AGENTSVIEW_DATA_DIR"
make dev
```

---

## 9. 常见问题

### `air not found`

```bash
make air-install
# 确保 $(go env GOPATH)/bin 在 PATH 中
```

### CGO / sqlite 编译失败

- macOS：`xcode-select --install`
- 确认未强制 `CGO_ENABLED=0`
- `make build` 会设置指向 go-sqlite3 头文件的 `CGO_CFLAGS`

### 前端 `npm ci` 失败

- 需要能访问 GitHub（`@kenn-io/kit-ui` 为公开 git 依赖）
- PATH 上要有 `git`
- Node 版本过旧时请升级到 22+

### 开发时 API 404 / CORS / 闪退到设置页

- 确认 `make dev` 已在 `:8080` 就绪
- 浏览器应打开 **Vite `:5173`**
- 经端口转发/反代访问时，用
  `agentsview serve --public-url <浏览器里的完整 origin>`

### 端口占用

```bash
# 查看并停止本工具后台
agentsview serve status
agentsview serve stop
agentsview daemon status
agentsview daemon stop
```

或换端口：`./agentsview serve --port 9090`。

开发模式换端口示例（`Makefile` 的 `ARGS` 会传给 air，再交给二进制）：

```bash
make dev ARGS='--port 9090'
VITE_API_TARGET=http://127.0.0.1:9090 make frontend-dev
```

### 「旧启动脚本」行为

以下做法**已过时**，请勿再依赖：

- 手写 `.agentsview.pid` / `.agentsview-go.pid` 管理进程
- 开发模式直接 `nohup ./agentsview serve`（无 air，改 Go 不重载）
- 只起后端却打开 `:5173`（或只起 Vite 不起后端）

请改用第 3–5 节与当前 `start-agentsview*.command`。

---

## 10. 与 Makefile 目标对照

```text
make build           完整 debug 二进制（含前端）
make build-release   优化/strip 后的发布构建
make install         build-release 并安装到 PATH
make frontend        仅构建并嵌入前端
make frontend-dev    Vite 开发服务器
make dev             air 热重载 Go serve
make air-install     安装 air
make desktop-dev     Tauri 桌面开发
make test / lint / e2e / vet
make help            打印全部目标
```

更细的 CLI 子命令见 `docs/commands.md`；配置项见 `docs/configuration.md`。
